
#include <fmt/format.h>

void runDynamo()
{
    auto window = window::Window(
        fmt::format("Dynamo {}.{}.{}.{}", VERSION_MAJOR, VERSION_MINOR, VERSION_PATCH, VERSION_TWEAK);
        , {1200, 1200}
    );
    renderer::Renderer();
    // initalize renderer



    // TODO: create world


    while (true)
    {

    }

}